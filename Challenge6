//edited open source code to determine factorial of n in java. the method i added:

private int factorial (int n) {
  if (n <= 0){
  return 0;
  }
  
  int i = 1;
  while (i <= n) { 
    n *= i
    i++;
  }
  return n;
  
}
